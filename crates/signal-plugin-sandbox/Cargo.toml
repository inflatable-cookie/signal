[package]
name = "signal-plugin-sandbox"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Out-of-process plugin container shell for Signal."

[dependencies]
signal-ipc = { path = "../signal-ipc" }
