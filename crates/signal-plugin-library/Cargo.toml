[package]
name = "signal-plugin-library"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Canonical plugin library domain for Signal consumers."

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }