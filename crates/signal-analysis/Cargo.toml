[package]
name = "signal-analysis"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Shared analysis traits, result types, and confidence models for Signal."

[features]
# Corpus/acceptance/regression harness used by sibling analysis crates' tests.
# Off by default so production consumers never compile test infrastructure.
test-support = []

[lints]
workspace = true

[dependencies]
signal-dsp-resample = { workspace = true }
signal-primitives = { workspace = true }
