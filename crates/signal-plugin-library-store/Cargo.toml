[package]
name = "signal-plugin-library-store"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Storage traits for Signal plugin inventory and library consumers."

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
signal-plugin-inventory = { path = "../signal-plugin-inventory" }
signal-plugin-library = { path = "../signal-plugin-library" }