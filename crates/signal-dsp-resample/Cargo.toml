[package]
name = "signal-dsp-resample"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Deterministic offline and chunked mono resampling helpers for Signal."

[dependencies]
signal-primitives = { path = "../signal-primitives" }