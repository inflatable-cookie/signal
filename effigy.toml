[catalog]
alias = "signal"

[tasks]
health = [{ task = "build" }]
validate = [
  { task = "health" },
  { task = "test" },
]
qa = [{ task = "validate" }]
build = [
  { task = "configure:debug" },
  { task = "compile:debug" },
]
dev = [
  { task = "build" },
  { run = "./build/src/loophole-signal {args}" },
]
test = [
  { task = "build" },
  { run = "ctest --test-dir build --output-on-failure {args}" },
]
"configure:debug" = "cmake -S . -B build"
"compile:debug" = "cmake --build build --config Debug"
"audit:god-files" = "python3 ../chorus/meta/tools/check_god_file_audit.py --workspace-root .. --fail-on high"
