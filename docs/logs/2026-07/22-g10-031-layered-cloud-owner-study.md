# g10.031 LayeredCloud Owner Study

Date: 2026-07-22
Batch: 31.69
Result: complete; one isolated candidate ready

## Result

Frozen one clean-room `LayeredCloud` renderer for continuous fixed creative
expansion from `16x` through `100x`. It is a pointer-led granular system with
one exact source map, one deterministic launch lattice, bounded variable-
length unit-rate grains, channel-shared validity normalization, linked-stereo
sample ownership, the admitted Dream exterior envelope, exact crop, and
duration-independent working state.

Csound `sndwarpst` supplies the complete source-backed architecture family.
SuperCollider `Warp1` confirms that family but keeps randomized active-grain
state per output channel, so it is not the linked-stereo authority. No source
expression, constants, tables, random generator, or control flow transferred.

The brief freezes Contract `085` Rule 11 structural and synthetic owners,
long-form mono comparison against PaulXStretch and Csound, independent stereo
listening, rejection cleanup, and the minimal admissible production surface.
No candidate DSP, harness, experiment module, route, control, cache,
dynamic-ratio, Loophole, or Chorus surface entered `main`.

## Boundary Decision

Cloud can render every future `16x..32x` upper-overlap probe and shares the
Contract `085` map and admitted Dream exterior envelope. The overlap remains
paused because admitted `DirectRenewalDream` supports exact `16x` but no
interior ratio. One shared endpoint does not authorize a blend.

## Risks

- audible grain periodicity or tonal cancellation despite bounded duration
  variation
- entry and release character at material edges
- output-dominated cost at `100x`
- natural-stereo image and balance under independent listening
- no upper-overlap path until Dream gains separately admitted continuous-ratio
  authority

## Next Task

Run Batch 31.70 only. Implement the frozen authority once in worktree
`signal-candidate-31-70` on branch `candidate/g10-031-layered-cloud`. Pass two
complete unchanged conformance rounds before freezing the acoustic checkpoint,
then run `Y01..Y05`. Keep `main`, admitted renderers, overlaps, routing,
controls, cache, dynamic ratio, Loophole, and Chorus unchanged. Do not push.
