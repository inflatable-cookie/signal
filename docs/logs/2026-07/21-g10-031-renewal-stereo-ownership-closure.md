# g10.031 Renewal Stereo-Ownership Closure

Date: 2026-07-21
Batch: 31.37
Status: complete; renewal closed, PaulX-like product target retained

## Result

The two complete renewal candidates failed linked stereo for the same dominant
cause. Batch 31.25 inverted global source balance. Batch 31.36 passed compile,
construction, structural, synthetic, and concealed mono admission, then
failed local mapped-window balance at `16x`: about `2.00 dB` on bass and
`9.37..9.42 dB` with channel-dominance reversal on the full mix.

The Batch 31.36 native-channel law already preserves each frame's left/right
magnitudes and exact complex relation at `space=0`; algebraically it applies a
common current-frame rotation. The failure appears after inverse synthesis and
adjacent-frame blending because independently renewed common phases do not own
the interference between frames. This is temporal waveform ownership, not a
missing same-frame relation formula.

## Source Reassessment

Retained source evidence does not support another renewal-local repair:

- Bungee adds predecessor-driven common peak-region rotation.
- Signalsmith adds coherent horizontal and vertical recurrence, with its
  published path incomplete above `2x`.
- Rubber Band R3 adds tracked peak, material, and multiresolution state.
- SBSMS supplies paired oscillators, but the pinned source family already
  failed Signal's mono, stereo-mechanics, and quality feasibility boundary.
- PaulX uses independent channel renewal. It defines the desired diffuse
  character, not the current hard source-relative local-image invariant.

Each viable temporal correction therefore selects a different coherent
family already assessed or closed. Windowed gain, covariance projection,
relation smoothing, dormant state, phase tweaks, and `space` changes are local
repairs without a supported complete target or bounded convergence law.

## Decision

Renewal closes without promotion under the current Contract `085` stereo
boundary. PaulX-like `Dream` remains product intent. No successor brief,
candidate worktree, DSP, tests, harness, fixture, API, route, cache, Loophole,
or Chorus surface was created or changed.

The lane is paused at an operator intent checkpoint. The current contract
keeps local source-relative stereo terminal. Changing it would require an
explicit product decision that creative stereo may instead promote through
hard integrity and global-balance controls plus comparator-relative
independent listening, with mapped-window source relation retained as a
diagnostic.

## Validation

- `git diff --check`
- `effigy qa:docs`
- `effigy qa:northstar`
- `effigy health`
- `effigy validate`

All passed. `effigy doctor` retains the pre-existing god-file error and
attention-marker warning; this batch did not expand scope to address them.

## Next Task

Choose the creative-stereo promotion policy. If local source-relative stereo
remains terminal, keep renewal closed and reassess the product target. If
comparator-relative creative image is authoritative, revise Contract `085`
first, then freeze a fresh complete candidate. Do not recover rejected code,
start implementation, or reopen the transparent successor before that choice.
