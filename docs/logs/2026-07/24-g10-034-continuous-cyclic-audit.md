# g10.034 Continuous Cyclic Audit

Date: 2026-07-24
Batch: 34.1
Status: complete

## Decision

Select one `ContinuousEventLedgerCyclic` evidence candidate over every integer
target `2N <= T <= 8N`.

The admitted renderer has no ratio branch inside that interval. Target frames
alter one rational centre map, positive anchor advance, and planned replica
spacing. Cycle conversion, complementary window, native-rate reads,
interpolation, exterior zero, exact output, linked-channel schedule,
deterministic state, and bounded memory remain unchanged.

Exclude `N <= T < 2N`. Identity is a special byte-copy bypass, and no public,
comparator, or listening anchor owns the effect's emergence immediately above
it.

## Evidence Boundary

Private identity-through-`8x` acceptance remains non-promotional. Public and
acoustically admitted Cyclic remain exact `2x`, `4x`, and `8x`.

Batch 34.2 must freeze:

- byte-exact anchor parity
- one-frame-adjacent, fractional, integer-interior, and invalid-boundary targets
- full structural and `Y01..Y06` synthetic ownership
- matching interior ReaReaRea rows
- concealed five-family mono and linked-stereo listening
- a fresh independent-stereo decision or a new checkpoint-scoped Rule 5 waiver
- rejection, cleanup, deterministic cost, memory, and minimal admission

## Scope

Documentation only. No DSP or test execution, candidate harness, comparator
capture, listening asset, public API, routing, cache, artifact, runtime, UI,
Loophole, or Chorus surface changed.

## Validation

- `git diff --check`: pass
- `effigy qa:docs`: pass
- `effigy qa:northstar`: pass
- `effigy health`: pass
- `effigy validate`: pass

## Next

Execute Batch 34.2 only. Freeze the complete implementation and evidence brief
without changing or running DSP.
