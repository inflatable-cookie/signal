# g10.031 Renewal Evidence-Authority Closure

Date: 2026-07-21
Batch: 31.40
Status: complete; renewed candidate path closed

## Result

The Batch 31.36 `9/9` and Batch 31.39 `7/9` synthetic receipts cannot be
compared as identical executable evidence. Both checkpoints remain rejected
and deleted. Renewal implementation closes again; the PaulX-like neutral
`Dream` target remains valid without a ready Signal renderer.

## Audit

Exact shared authority:

- renderer formulas at documentation level
- counter tags, vectors, and `ADMISSION_SEED`
- synthetic source and `SYNTHETIC_SUPPORTS` definitions
- owner IDs, names, counts, and high-level boundaries

Missing executable identity:

- candidate source and helper bodies
- numeric evaluation and row-assembly details
- exact assertion bodies and helper digests
- complete per-row Batch 31.36 synthetic values
- candidate output or golden-output digests

Construction `1/1` verified inventory and selected constants. It did not prove
that two fresh implementations contained the same helpers or assertions.

`Y04` is canonically one region with `None` secondary. Its `-30 dB` value
selects active RMS windows; it is not a secondary-peak ceiling. Batch 31.39's
two-region `16x` impulse row therefore rejects that checkpoint, but the deleted
Batch 31.36 output cannot be compared.

`Y09` never froze one executable source-relative swap assertion. The pair law
explicitly declines exact time-domain swap at the negative-real half-angle
branch. `S12` instead names swapped magnitudes, conjugate relation, and decoded
image. The inherited `Y09` prose does not define the exact long-form swap
fixture, estimator, tolerance, or assertion behind Batch 31.39's `swap 4x`
and `swap 8x` messages.

The authority chain also spans four briefs with later `Y02`, `Y08`, and stereo
policy deltas. It never became one self-contained executable gate definition.

## Decision

- Batch 31.36's pass is historical evidence for its checkpoint only.
- Batch 31.39's failure is terminal for its checkpoint only.
- Neither receipt proves or invalidates the other checkpoint.
- Recovering deleted source is forbidden.
- Writing a new self-contained brief and running it would be a third renewal
  candidate, not reconciliation.
- No materially different source-backed renewal owner justifies that work.
- Further renewal implementation is closed. Product exposure, routing, other
  characters, Loophole, and Chorus remain closed or paused.

Repo posture is `baseline-routing`; no strict lane is active. `g10.031` is
paused with no implementation batch ready.

## Validation

- `git diff --check`
- `effigy qa:docs`
- `effigy qa:northstar`
- `effigy health`
- `effigy validate`

All passed. `effigy doctor` retains the pre-existing `57` god-file findings
and `5` attention markers; this batch did not expand scope to address them.

## Next Task

No implementation batch is ready. The operator must explicitly choose whether
to commission research for one materially different, source-backed complete
creative owner or leave `g10.031` paused. Do not recover deleted checkpoints,
run another renewal candidate, change gates, open routing or product exposure,
touch Loophole or Chorus, or push.
