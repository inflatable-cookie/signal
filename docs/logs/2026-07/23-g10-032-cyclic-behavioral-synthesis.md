# g10.032 Cyclic Behavioral Synthesis

Date: 2026-07-23
Status: Batch 32.3 complete; Batch 32.4 ready

## Result

- selected centred compressed-anchor Cyclic behavior
- rejected raw whole-cycle repeat/jump as the primary ReaReaRea-like target
- froze one manual render-wide `cycle` as sufficient first-character ownership
- classified automatic cycle selection as later specialist assistance, not
  adaptive `INTELL`
- froze `duration`, `character=Cyclic`, and `cycle` as the minimum UI
- corrected the old absolute pitch gate

The future renderer owns one monotonic ideal map, forward native-rate waveform
reads, compressed source-anchor progress, ratio-dependent replica clusters
centred around mapped events, one linked schedule, and exact boundaries.

## Gate

Hard:

- length, finiteness, silence, determinism, bounded state
- monotonic map and forward reads
- normalized crop and exterior continuity
- terminal overlap state
- commanded-replica ledger
- linked stereo mechanics
- exact `16x` pre-allocation rejection

Diagnostic:

- pitch and comparator delta
- replica count, spacing, centre, and extent
- cadence, sidebands, crest, level, dropout, tail, and local stereo balance

Missing or non-finite evidence rejects. Finite character differences go to
concealed listening across the retained five-family `2x`/`4x`/`8x` pack.

## Boundary

Both historical candidates remain rejected and deleted. The comparator-backed
gate change addresses `CyclicGrain`'s sole acoustic failure class under
Contract `085` Rule 11. It does not restore deleted code or authorize a
constant sweep.

No DSP, candidate module, harness, fixture, report mode, public API, route,
cache, Loophole, or Chorus change entered `main`.

## Remaining Risk

- exact cycle range and default are uncalibrated
- join and boundary geometry may trade clicks against softened attacks
- short cycles may over-shift pitch or buzz
- long cycles may echo, stutter, or pump
- musical sufficiency and independent Cyclic stereo remain untested

## Validation

- `git diff --check`
- `effigy qa:docs`
- `effigy qa:northstar`
- `effigy health`
- `effigy validate`

## Next Task

Execute `g10.032` Batch 32.4 only. Freeze one complete centred
compressed-anchor Cyclic renderer and its Contract `085` Rule 11 evidence
authority. Do not implement candidate DSP.
