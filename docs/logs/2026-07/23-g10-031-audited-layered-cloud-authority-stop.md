# g10.031 AuditedLayeredCloud Authority Stop

Date: 2026-07-23
Batch: 31.73
Status: complete; stopped before structural admission, Cloud closed

## Result

Created the exact fresh worktree `signal-candidate-31-73` and branch
`candidate/g10-031-audited-layered-cloud` from `32c5a83c`. Candidate assembly
was source-clean. Batch 31.70 source, output, helpers, receipts, and build state
were not recovered.

The private renderer, nine-spec authority surface, tracked manifest, and
nextest profile compiled. Construction passed `1/1`, including the frozen
source hashes, vector hash, `134`-row manifest, owner inventories, receipt
schema, deadlines, and comparator/listening bindings.

The `S03` occupancy proof then found contradictory authority before a complete
structural round:

- support is strict: `2|q|<D`
- duration is bounded by `D<=20H`
- an open interval of width at most `20H` contains at most `20` regular
  `H`-spaced launch centres
- one distinct terminal launch raises the maximum to `21`
- the brief and exact vector require observed maximum `22`

`21` is attainable: use a non-grid output residue with `D=20H` for `20`
regular launches and place a distinct active terminal at the same output
frame. `22` is not attainable without changing a frozen support predicate,
duration bound, or assertion.

No implementation-time repair was permitted. No candidate commit, checkpoint,
acoustic ref, synthetic render, comparator output, or listening pack exists.
Admitted DSP, routing, controls, cache, dynamic ratio, Loophole, and Chorus
were unchanged.

This is the second Cloud evidence-integrity failure. Contract `085` closes the
family without another authority rebinding. After this docs closeout, delete
the disposable worktree, branch, source, tests, and build state.

## Next Task

Run Batch 31.74 only. Reassess the creative high range docs-first. Decide
whether `16x..100x` remains active, narrows to admitted coverage, or warrants a
materially different complete-owner study. Do not repair or rebind
LayeredCloud.
