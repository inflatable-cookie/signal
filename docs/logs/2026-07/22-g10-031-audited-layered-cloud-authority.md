# g10.031 AuditedLayeredCloud Authority Freeze

Date: 2026-07-22
Status: complete; docs only
Scope: Batch 31.72

## Result

Froze one source-clean `AuditedLayeredCloud` brief around the unchanged
pointer-led granular renderer. The new authority does not recover or use the
deleted Batch 31.70 source or output. No candidate DSP, harness, fixture,
comparator render, listening pack, product surface, Loophole, or Chorus change
entered `main`.

The brief now binds nine compile-linked specs across renderer, source, exact
vectors, evidence, memory, runner, comparator, listening, and cleanup. One
manifest maps every row, assertion, diagnostic, receipt field, deadline, and
terminal decision to executable ownership.

## Closed Gaps

- exact source hashes and counter/map/launch/interpolation/boundary vectors
- allocation-free invalid rejection and derived exhaustive grain occupancy
- denominator, fixed summation, boundary, mono-decode, and planned-capacity
  assertions
- one nextest process per row with enforced `120 s` structural, `900 s`
  synthetic, and `7200 s` capture deadlines
- atomic per-row receipts with truthful frame/channel arrays and specific
  assertion/diagnostic inventories
- `Y02` component energy presence plus persisted pitch; `Y03` centroids;
  `Y04` cross-block dropout and final remainder; `Y05` whole/band/window stereo
- toolchain-checked PaulX/Csound capture, exact level matching, concealment,
  `30` listening rows, `90` renders, mono arithmetic, and eligible independent
  stereo decisions
- rejection cleanup, second-integrity-failure closure, and minimal pass surface

## Decision

Batch 31.73 is genuinely ready for one fresh isolated implementation. Acoustic
identity remains closed until compile, construction `1/1`, and all `101`
structural rows/`51` renders pass twice unchanged from one clean tree. Any
unanswered authority choice returns to docs. Any post-checkpoint miss rejects.

## Validation

- `git diff --check`
- `effigy qa:docs`
- `effigy qa:northstar`
- `effigy health`
- `effigy validate`

All passed. `effigy doctor` remains limited to the known god-file and
attention-marker findings recorded before this batch; they are out of scope.

## Next Task

Run Batch 31.73 only. Create the exact fresh isolated worktree and implement
the frozen `AuditedLayeredCloud` brief. Do not recover Batch 31.70 source or
output, run acoustic work before the ref, touch admitted DSP or product
surfaces, work in Loophole or Chorus, or push.
