# g10.031 Capacity-Audited Linked STN Authority Rejection

Date: 2026-07-21
Batch: 31.47
Result: stopped before checkpoint; candidate deleted

Batch 31.47 started from `4fd15e5b` in the exact fresh worktree and branch.
Candidate assembly remained private. Before compile or construction execution,
the exhaustive geometry audit found a second contradiction in the frozen
authority.

At `F=8000`, the frozen geometry gives `N_t=2048`, `N_s=256`, and `A_s=64`.
The short vertical extent is therefore
`round(1800*N_s/F)=round(57.6)=58`. The frozen nearest-odd rule resolves the
equal-distance `57`/`59` choice upward, producing `R_v=59`. The same brief
requires construction to prove `R_v<=57` over every supported sample rate.

Fixing either side would change a frozen DSP literal, tie rule, or assertion,
outside the permitted pre-checkpoint repair class. Compile, construction,
structural, synthetic, and listening gates did not run. No checkpoint,
candidate tree, or receipt exists. The worktree, branch, private source, tests,
and candidate state were deleted. Main received documentation only.

This is an authority failure, not renderer-quality evidence. Batch 31.48 must
recompute every geometry-derived median bound and its memory consequences
docs-only before any fresh linked-STN implementation is considered.
