# g10.032 Cyclic Event-Ledger Authority

Date: 2026-07-24
Status: Batch 32.18 complete; Batch 32.19 ready

## Change

One canonical
`EventLedgerAuditedCenteredCompressedAnchorCyclic` brief now owns the complete
candidate. Renderer, map, cycle, two-read crossfade, linked-channel,
boundary, exact-length, memory, cost, and determinism formulas are unchanged.

Y01 rows `012..017` are sparse-event rows. They replace continuous dropout
with independent-ledger presence, exterior-zero, order, centre, and measured
event diagnostics. The other `24` Y01 rows retain the exact `221`-frame,
`-40/-80 dBFS` dropout gate.

The manifest remains `588` rows: one construction, `339` structural, `183`
synthetic, five exact-`16x`, and `60` long-form render rows.

## Evidence Authority

Construction must execute hard known answers through the real:

- FFT and pitch estimator
- event-ledger and sparse assertion dispatcher
- cadence FFT, strength, autocorrelation, and cycle-order summary
- gap, dropout, tail, and exterior analyzers
- linked-stereo balance, bands, algebra, and correlation
- level match and listening fade
- comparator WAV, project, manifest, decode, and hash verifier

Authored values, placeholder diagnostics, owner labels, pointer-presence
checks, and self-comparisons fail construction.

## Boundary

No candidate worktree, source, harness, comparator render, acoustic receipt,
DSP change, public API, or product route entered `main`. Rejected candidate
code and evidence remain deleted.

## Next Task

Execute Batch 32.19 only. Create the frozen isolated identity, bind evidence
before candidate source, implement the brief without rejected-code recovery,
and complete both unchanged conformance rounds. Stop before `Y01`.
