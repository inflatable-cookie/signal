# g10.032 Cyclic Event-Ledger Conformance

Batch 32.19 is complete.

Fresh evidence commit `6a909c74` bound `588` executable rows, `63` regenerated
ReaReaRea comparator rows, and `51` listening rows before candidate source.
Private candidate commit `08e5c57c` implemented the centred compressed-anchor
two-read renderer and its compile-linked evidence owners.

The first checkpoint stopped before construction or DSP. The mandated
`/usr/bin/python3` is 3.10 and rejected the runner's Python 3.11-only
`zip(strict=True)`. The failed execution root contains zero receipts.
Evidence-only commit `995ea516` replaced it with an explicit field-count
check. Renderer and test logic did not change.

Checkpoint `995ea516`, tree `fd42543b`, then passed release compile,
construction `1/1`, and all `339` structural rows twice. Both rounds completed
`168/168` planned renders. Receipt trees and summaries are byte-identical.
Structural summary SHA-256:

`f1e90cd36557d1c1b6ef3be70175b7f025cdd00f1d7405a7a7958cf8a91cb08b`

The local `32-19-acoustic` ref points to `995ea516`. No acoustic, exact `16x`,
long-form, or listening row ran. Candidate code and ignored evidence remain
outside `main`.

Next: Batch 32.20 runs only the frozen `30`-row Y01 gate once and stops before
Y02.
