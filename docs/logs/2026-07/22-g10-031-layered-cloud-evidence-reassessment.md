# g10.031 LayeredCloud Evidence Reassessment

Date: 2026-07-22
Batch: 31.71
Status: complete; one fresh docs-first identity justified

## Audit

Inspected checkpoint `ee42f50c4c338db4af8a7feaa89bb8b21e8d0860`, tree
`cfc28c8c6c4095f0c91ae95d0724962656bcec97`, and its receipts without
executing candidate DSP.

The evidence failure extends beyond the already-recorded `Y05` omission:

- canonical render, source, and memory spec owners were absent
- construction counted labels and three partial oracles; it did not bind every
  assertion, receipt field, comparator, or listening edge
- no tracked `layered-cloud` nextest profile existed; row deadlines were
  constants without enforcement
- `S02..S08` omitted frozen map, occupancy, sampling, normalization, boundary,
  mono-decode, or planned-capacity assertions
- stereo `output_frames` counted interleaved samples and assertion payloads
  named no specific checks
- `Y02` had no meaningful component-presence proof or persisted pitch values
- `Y03` omitted persisted centroids
- `Y04` did not scan dropout windows across one-second block boundaries
- `Y05` omitted three-band and mapped-window analysis and persisted no natural-
  stereo diagnostics
- comparator capture, concealment, level matching, the `30` listening rows,
  `90` renders, and terminal decisions had no compile-linked owner

The checkpoint cannot be repaired or rerun. It has no valid synthetic,
listening, or quality result.

## Decision

This is the first incomplete-evidence identity for a small source-backed Cloud
renderer. No valid acoustic result exists and no output may guide a
replacement. Contract `085` now permits exactly one fresh docs-first
`AuditedLayeredCloud` identity.

Batch 31.72 must freeze a complete replacement brief from canonical
architecture and this gap ledger before implementation. It must resolve the
component-presence rule, row-timeout mechanism, truthful receipt semantics,
and complete comparator/listening ownership. A second evidence-integrity
failure closes the family.

After this docs decision commits, delete the Batch 31.70 worktree, branch,
build state, generated output, and local evidence ref. No candidate source or
output enters `main` or the replacement.

## Next Task

Run Batch 31.72 only. Freeze one complete `AuditedLayeredCloud` brief. Do not
implement DSP, recover Batch 31.70 source/output, or change admitted renderers,
overlaps, routing, controls, cache, dynamic ratio, Loophole, or Chorus.
