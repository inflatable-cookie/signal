# g10.031 Linked STN Complete-Owner Study

Date: 2026-07-21
Batch: 31.41
Status: complete; `LinkedStnNoiseMorph` selected for complete brief

## Scope

Execute the operator-authorized search for one materially different,
source-backed complete creative stretch owner. Change documentation only. Do
not recover renewal code, implement a candidate, add evidence surfaces, or
touch product routing, Loophole, or Chorus.

## Baseline

- branch: `main`
- starting commit: `cd00be5d Close creative renewal evidence authority`
- worktree: clean
- prior renewal checkpoints and branches: absent by required cleanup

## Source Decision

Pinned SiTraNoStar `v2.0.1` at
`2edf7b693040b5070116299973abf83dc5ba86e5` changes the previous STN evidence
boundary. It is a runnable classical whole path through two-stage
sines/transients/noise decomposition, tonal phase locking, transient
relocation, noise morphing, recombination, playback, and export.

It is not candidate authority. The GPL-3.0 source is clean-room evidence only,
and the application is mono-only, nondeterministic, full-file,
approximate-length, and publicly ranged only through `10x`. The Noise Morphing
paper's blind evidence uses short mono material at `2x`, `4x`, and `8x` and
explicitly leaves stereo extension open. The related neural STN path has no
released complete training/weight authority and is excluded from Signal.

The complete triangulation supports one different family:
`LinkedStnNoiseMorph`.

- one exact map owns every material lane
- linked persistent tonal phase owns pitch and sustained coherence
- shared native-channel transient events move once and own replicas and
  placement
- continuous deterministic multichannel residual excitation owns dream-like
  diffusion and stereo statistics
- one mapped envelope, reconstruction, normalization, and crop own entry/tail
  energy and exact length
- bounded state and fixed offline cost remain mandatory

This confines phase forgetting to separated residual noise. It does not repeat
renewal's complete-mixture stochastic frames or repair a rejected checkpoint.

## Risk

The source evidence does not establish long-form music, `16x`, linked residual
width, perfect component recombination after independent time treatment,
exact boundaries, deterministic bounded execution, or competitive PaulX-like
listening. Those remain terminal Signal gates. Independent stereo listening
remains required after objective and concealed mono admission.

## Repository Result

- docs/research/architecture/contract/roadmap authority updated
- no DSP, test, harness, fixture, API, route, cache, or product code changed
- `OfflineHighQuality`, Contract `084`, Loophole, and Chorus untouched
- no external production dependency introduced

## Validation

- `git diff --check`: pass
- `effigy qa:docs`: pass
- `effigy qa:northstar`: pass
- `effigy health`: pass
- `effigy validate`: pass
- `effigy doctor`: expected pre-existing god-file and attention-marker
  findings only

## Next Task

Run Batch 31.42 only. Freeze one self-contained clean-room
`LinkedStnNoiseMorph` renderer brief with no decide-later ownership gaps. Stop
before candidate implementation.
