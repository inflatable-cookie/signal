# g10.032 Cyclic Executable Forensics

Date: 2026-07-23
Status: Batch 32.2 complete; Batch 32.3 ready

## Result

- captured the missing five ReaReaRea musical `2x` rows
- captured ReaReaRea impulse, tone, chord, noise, and stereo rows at
  `2x`, `4x`, and `8x`
- ran `144` Potenza slow-anchor and `144` SickoCV repeat/jump rows across
  short, source-default-region, and long observation cycles
- ran nine pinned Sonic specialist rows
- verified the Potenza translation against the pinned header within
  `5.9604645e-8`
- promoted one `345`-row receipt into the canonical Cyclic dossier

## Finding

The schedules are objectively distinguishable:

- SickoCV repeats each event exactly `ratio` times at fixed output spacing
- Potenza produces more overlapping appearances than the integer repeat count
- ReaReaRea scales replica count like compressed-anchor overlap, but centres
  the replicas around mapped events
- ReaReaRea tail support also scales closer to the long-cycle Potenza probe
  than to raw repeat/jump

No Signal renderer is selected. The centred map, boundary law, cycle
vocabulary, and perceptual sufficiency remain Batch 32.3 decisions.

## Gate Correction Evidence

ReaReaRea's strongest component on the `110 Hz` probe is about `+114` cents
at `2x` and `+120` cents at `8x`. The deleted candidate's absolute `15`-cent
gate did not describe the target.

All three shared-cycle schedules preserved the tested linked-stereo
relationships within `0.001508 dB`. Sonic remained finite but missed exact
target length on all nine rows.

## Boundary

The batch added no Signal DSP, candidate module, harness surface, fixture,
public API, route, cache, Loophole, or Chorus change. All executable tools,
renders, build state, and raw measurements remained ignored or external, then
moved to recoverable Trash after receipt promotion. The retained comparator
corpus was not changed.

## Validation

- `git diff --check`
- `effigy qa:docs`
- `effigy qa:northstar`
- `effigy health`
- `effigy validate`

All passed. `effigy doctor` retains only the pre-existing god-file errors and
attention-marker warning.

## Next Task

Execute `g10.032` Batch 32.3 only. Synthesize the receipt into one behavioral
decision and corrected future gate. Record any listening uncertainty
explicitly. Do not implement a Signal renderer.
