---
title: Linux CLAP discovery worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Inflatable Cookie
created: 2026-08-21
updated: 2026-08-21
handoff_path: /Users/tom/Dev/projects/signal/docs/handoffs/20260821-141416-g04-136-linux-clap-discovery.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr]
---

## What This Thread Was Doing

The Soundcheck orchestrator closed roadmap 028 (card 135 merged as soundcheck
`09065160`) and opened the Linux lane. Your job is Soundcheck card 136:
implement Linux CLAP filesystem discovery in `signal-plugin-clap` exactly as
Signal contract 086. This is a product-pulled backlog item, not a Signal
generation.

This is the handoff from the orchestrator to one bounded implementation
thread in the `signal` repository. Start from this file; no transcript or
second prompt is needed.

## Why It Matters

Soundcheck's Linux lane (g04.029) is serial: Signal discovery first, then
`soundcheck-library` scan orchestration (137), then the app (138). Until
Signal can find Linux CLAP units, nothing downstream can scan real plugins on
Linux.

## Current State

Here is the state the worker is inheriting:

- **Repository:** `signal`
- **Planning branch:** `main`
- **Planning base commit:** `ba33fca96a22745ebb6551a116d5706a9c92f72a`
  (contract 086)
- **Pushed main verification:** `origin/main` was `ba33fca9` when this
  handoff was written (planning commit, before the handoff commit)
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight.
- **Planning artifacts included at the base:** Signal contract 086 active;
  Soundcheck contract 030 and card 136 `ready` on soundcheck `main`
- **Worker branch:** `worker/g04-136-linux-clap-discovery`
- **Worker worktree:** `/Users/tom/Dev/worktrees/signal-g04-136`
- **Worktree creation command:** `git fetch origin && git worktree add -b worker/g04-136-linux-clap-discovery /Users/tom/Dev/worktrees/signal-g04-136 origin/main`
- **Worktree policy:** first use the clean, dedicated, non-`main` registered
  worktree supplied by the launcher, even if its generated path or branch
  differs from these handoff placeholders. Record the actual path/branch and
  never create a second worktree for that reason. If the current context is
  unusable, use the named worktree when it matches; only then read
  `.agents.local.env` (`AGENTS_WORKTREE_CONTAINER_DIR=/Users/tom/Dev/worktrees`
  on this machine) and create a unique manual worktree/branch under that
  container from `origin/main`. Never use `/tmp`, `TMPDIR`, or a guessed
  path.
- **Active spec lane:** none; Soundcheck card 136 is the ready execution card
- **Card:** soundcheck `docs/roadmaps/g04/batch-cards/136-g04-signal-linux-clap-discovery.md`
- **Allowed runway:** card 136 only; do not implement Soundcheck 137/138
- **Remaining card budget:** one card
- **Dispatch topology:** serial; library and app workers wait for this merge
- **Canonical refs:** signal contract 086 (authoritative for scope); Signal
  contracts 022, 072, 083 for existing discovery shape; Soundcheck contract
  030 for the consumer boundary
- **Tool/runtime restrictions:** development on this macOS machine; Linux
  behaviour is proven with cfg-gated unit tests and rustc fixtures
  (`compile_clap_fixture`), not by requiring a live Linux host. macOS CLAP
  discovery must not regress.
- **Required validation:** `cargo test -p signal-plugin-clap` (all layouts);
  `effigy validate` or the repo's cheap baseline; `effigy qa:docs` if docs
  change
- **PR base/head:** `main` ← `worker/g04-136-linux-clap-discovery`
- **PR URL:** pending
- **Review state:** awaiting-review after the PR exists
- **Merge authorisation:** operator only; do not merge

## Boundaries

Please keep this run inside the named runway:

- **In scope:** contract 086 exactly — Linux default roots (`~/.clap`,
  `/usr/lib/clap`, `/usr/local/lib/clap`, then `CLAP_PATH` appended), flat
  `.clap` files, directory bundles via
  `Contents/{x86_64,aarch64}-linux/<stem>[.so]`, fixture tests for both
  shapes, macOS-only bundles excluded on Linux
- **Out of scope:** AU; VST3/LV2 discovery changes; Soundcheck or
  `soundcheck-library` edits; REAPER; hosting, sandbox, or GUI work; opening
  `g12`; release or CI mutations
- Do not treat `Contents/MacOS` as a Linux layout. A bundle with only
  `Contents/MacOS` is not a Linux unit.
- Do not invent architecture or change contract 086; if the contract is
  ambiguous or wrong in practice, stop and report through the operator.
- This handoff represents one worker lane. Work only in the selected clean
  worker worktree; never edit the orchestrator's planning checkout.
- Do not merge the PR. Merge remains a separate operator-authorised action.

## Important Context

- **Planning lineage:** Soundcheck g04.029 Linux scan and REAPER; Soundcheck
  contract 030; Signal contract 086. Signal g11 is complete; this is
  product-pulled backlog, not g12.
- **Why this card is ready:** Soundcheck card 135 merged and roadmap 028
  closed, so 136 is the single ready card. Signal PR #4 already made the
  Linux build compile; discovery semantics are the remaining gap.
- **Decisions and preferences:** consumers pass default roots to opt in — an
  empty explicit root list scans nothing, same as VST3/LV2. Test plugins are
  the rustc fixtures; vendor installers are not the unit-test spine.
  Soundcheck pins Signal tag `v0.1.1` and path-deps during the lane; tagging
  happens after merge, outside this card.
- **Open tensions:** the bundle-fixture test must match the host architecture
  (`aarch64-linux` on this machine's containers, `x86_64-linux` on Intel
  Grok); write the test so it compiles the fixture for whichever host runs
  it. macOS discovery tests must stay green on this machine.
- **Report after:** each meaningful chunk (roots exposed, bundle resolution,
  fixtures green), or immediately on any blocker
- **Report to:** the operator, who will relay progress to the orchestrator

## Suggested Next Move

This handoff explicitly activates worker mode. Start by reading it from the
top. Before broad repository reads, run the quick startup worktree-safety
preflight in `## Completion Protocol`. If the current context is a clean,
dedicated, non-`main` registered worktree, use it immediately and record its
actual path/branch. If it is `main`, dirty, or unregistered, use the named
worktree if it matches; otherwise create a unique worktree under
`AGENTS_WORKTREE_CONTAINER_DIR` from pushed `origin/main`. Never fall back to
`/tmp`.

Once that checks out, read contract 086, the related discovery contracts, and
the existing `signal-plugin-clap` scan-root and bundle-resolution code (VST3's
Linux handling from PR #4 is the nearest prior art). Implement roots, bundle
resolution, and fixture tests; validate; open the PR; stop.

## Completion Protocol

### Before you start

1. Read this handoff path. Its `worker_mode: implementation` and
   `dispatch_authority: orchestrator` metadata activate worker mode. Then run
   one quick read-only safety probe before broad repository reads:
   `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. If the current root is a registered worktree, its status is empty, and its
   branch is not `main`, accept it as the launcher-provided worktree. Record
   its actual root/branch and do not create another worktree merely because
   they differ from the placeholders above.
3. Only if the current context is `main`, dirty, unregistered, or otherwise
   unusable should you inspect the named worktree, then fall back to a unique
   worktree under `AGENTS_WORKTREE_CONTAINER_DIR` from pushed `origin/main`.
   Never clean, reset, or discard another checkout's dirty state; if the
   launcher supplied a dirty or `main` worktree, stop and report it.
4. From the selected worktree, confirm `git rev-parse HEAD` equals
   `git rev-parse origin/main`, confirm
   `git merge-base --is-ancestor ba33fca96a22745ebb6551a116d5706a9c92f72a HEAD`
   succeeds, and confirm this handoff file exists in the selected `HEAD`.
5. Read contract 086, `AGENTS.md`, and the canonical refs named above.
6. Run the repo's cheap orientation checks (`effigy tasks`, `effigy health`)
   and record what you actually ran.

### While you work

- Execute card 136 and keep commits aligned with meaningful chunks, not
  arbitrary model turns.
- After each meaningful chunk, report through the operator with changed
  files, validation actually run, and new risks or blockers.
- Stop and say so if the contract is ambiguous, scope expands, or validation
  changes the plan. Do not quietly widen discovery into hosting or sandbox
  work.

### When the assigned runway is complete

1. Run the required final validation: `cargo test -p signal-plugin-clap`
   covering Linux roots, the flat-file fixture, the host-architecture Linux
   bundle fixture, and the macOS-only-bundle exclusion; plus the repo
   baseline.
2. Push the selected worker branch.
3. Open a reviewable PR against the current pushed `main` tip. The handoff's
   `ba33fca9` is the planning base before the handoff commit, not the commit
   containing this file.
4. In the PR body, link contract 086, Soundcheck card 136, changed surfaces,
   validation, and unresolved items.
5. Report the PR URL to the operator. Do not merge. Card/roadmap closeout
   lives in the soundcheck repo and belongs to the orchestrator after merge.

### Review and merge path

The orchestrator reviews the PR against contract 086 and the diff. When the
orchestrator and worker share a GitHub identity, the verdict is posted as a
PR comment; that comment is the canonical review record. If changes are
requested, make only those changes on this branch, push, and report back.
Requested changes are: none yet. The operator must explicitly authorise any
merge.

### Handoff closeout

Leave the branch and PR honest. If the work is blocked, record the blocker
and stop rather than making the handoff look more complete than it is.
